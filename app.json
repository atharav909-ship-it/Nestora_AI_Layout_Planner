{
  "name": "bathroom-ai-designer",
  "description": "AI-assisted bathroom layout planner with interactive 2D and 3D views",
  "build": {
    "builder": "heroku/buildpacks:20"
  },
  "scripts": {
    "start": "uvicorn main:app --host 0.0.0.0 --port $PORT"
  }
}
